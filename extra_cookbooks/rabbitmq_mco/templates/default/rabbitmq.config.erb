%%
%% Generated by Chef for <%= node[:fqdn] %>
%%

[
	{rabbit, [
                {ssl_allow_poodle_attack, true},
                {vm_memory_high_watermark, <%= node.rabbitmq.high_memory_watermark %>},
		{ssl_options, [
			{cacertfile,"/etc/rabbitmq/ca.pem"},
			{certfile,"/etc/rabbitmq/cert.pem"},
			{keyfile,"/etc/rabbitmq/key.pem"},
			{verify,verify_peer},
			{fail_if_no_peer_cert,false}
		]}
	]},
   	{rabbitmq_stomp, [
		{tcp_listeners, [61613]},
        	{ssl_listeners, [61612]}
    	]}
].
