{% for key, value in config.items() | sort -%}
{% if key == 'topicprefix' -%}
topicprefix = /{{ value }}/
{% elif key in ('collectives',) -%}
{{ key }} = {{ ','.join(value) }}
{% else -%}
{{ key }} = {{ value }}
{% endif -%}
{% endfor -%}
